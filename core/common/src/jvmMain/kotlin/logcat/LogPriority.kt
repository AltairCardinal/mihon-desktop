package logcat

/**
 * JVM stub matching com.squareup.logcat.LogPriority.
 * On Android this comes from the logcat library; on JVM we stub it here.
 */
enum class LogPriority { VERBOSE, DEBUG, INFO, WARN, ERROR, ASSERT }
